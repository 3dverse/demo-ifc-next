import { defineStyleConfig } from "@chakra-ui/react";

const Spinner = defineStyleConfig({
    baseStyle: {
        color: "accent.500",
        borderWidth: "1px",
    },
});

export default Spinner;
